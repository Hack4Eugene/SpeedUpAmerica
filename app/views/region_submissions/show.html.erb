<% content_for :title, 'Speed Up Internet Region Speed Test Results' %>
<% content_for :description, 'See how fast your internet speeds are with the Speed Up internet speed test' %>

<div class='title-container'>
  <div class='background-container'>
    <%= render 'show_results', submission: @submission %>
  </div>
</div>

<% #= render 'mapbox', submission: @submission %>

<%= render 'all_results', all_results: @all_results %>

<%= hidden_field_tag :connection_type, @submission.testing_for %>
